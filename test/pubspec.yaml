name: platon_codec_test
version: 0.1.0
publish_to: 'none'

dependencies:
  platon_codec:
    path: ../
  ethereum_codec:
    git:
      url: https://github.com/nbltrust/dart-eth-transaction-codec
      ref: 0.2.8

dev_dependencies: 
  meta: 1.3.0
  test: 1.16.5

environment:
  sdk: ">=1.19.0 <3.0.0"
